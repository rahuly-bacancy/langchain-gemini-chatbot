info:
  name: Chat
  type: http

http:
  method: POST
  url: http://localhost:3000/chat
  body:
    type: json
    data: |-
      {
        "message": "Explain Angular Signals"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
